PORT=3000
SESSION_SECRET=change-me-to-a-long-random-string
NODE_ENV=development

# Controls whether Express trusts the proxy for secure cookies / client IPs.
# Set to '0' or 'false' to disable trusting the proxy (useful for local setups).
TRUST_PROXY=1
